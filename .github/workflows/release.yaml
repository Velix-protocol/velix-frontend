name: release

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  issues: write


jobs:
  release:
    runs-on: ubuntu-latest
    outputs:
      release_version: ${{ steps.set_release_version.outputs.NEXT_RELEASE_VERSION }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "20.8.1"

      - name: Set credentials of the current actor
        uses: ./.github/actions/setup-git-credentials
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Check Node.js version
        run: node -v

      - name: Install dependencies
        run: npm install

      - name: Run release
        run: npm run release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Set release tag as output
        id: set_release_version
        run: |
          echo "::set-output name=NEXT_RELEASE_VERSION::${{env.NEXT_RELEASE_VERSION}}"

      - name: Create release branch
        run: |
         branch_name="releases/${{ env.NEXT_RELEASE_VERSION }}"
         git checkout -b $branch_name
         git push origin $branch_name
        
